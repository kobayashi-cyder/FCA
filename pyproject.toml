[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fly-connectome-agent"
version = "0.1.0"
description = "A compact autonomous-agent architecture centered on sparse connectome-inspired computation."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["fca"]
